# macOS backends

CUDA packs in `backends/linux/` are **Linux Electron only**.

Metal llama-server lives under `mac/`:

- Install: `mac/scripts/install-llama-server.sh` (`brew install llama.cpp`)
- Run: `npm run start:macos`

Do not add CUDA / ROCm / Vulkan packs here.
